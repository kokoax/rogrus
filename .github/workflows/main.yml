name: sample

on: 
  schedule:
    - cron: "/5 * * * *"

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        git status
